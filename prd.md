# 局域网极速文件传输Windows端APP产品需求文档（PRD）

## 1. 产品构想

- **定位**：极简、极速、无广告的局域网跨设备文件传输Windows独立客户端（编译为单个独立 `.exe` 可执行文件，无需复杂安装）。
- **人群**：有频繁多设备大文件/文件夹传输需求、追求极致传输效率与 iOS 拟物美学 UI 的用户。
- **核心痛点**：传统局域网传输工具（如微信、飞书、LanDrop等）大文件传输慢、UI不够精美或操作层级过深。
- **差异化**：极致还原 iOS Liquid Glass（流体玻璃）高阶视觉，内置多线程 TCP/UDP 并行通道及 mmap 内存映射零拷贝技术，实现跑满物理网卡带宽的极致速度。
- **落地目标**：局域网千兆有线/Wi-Fi 6 环境下传输速度逼近物理带宽上限（100MB/s+），单次传输操作 3 步以内完成。
- **一期 MVP**：Windows 客户端极速互传（.exe独立运行）、自动发现局域网多设备（显示自定义昵称及头像）、点击头像选择特定目标发送文件/拖拽发送、进度浮窗。
- **二期迭代**：跨平台（Android/iOS/macOS）发现与互传、剪贴板共享、文件夹断点续传。

---

## 2. 极简 PRD (功能与交互)

### 2.1 模块划分
- **设备发现与身份识别模块**：基于 mDNS (Bonjour/224.0.0.251:5353) 与 UDP 广播双重自动发现机制，无须中心服务器。局域网内多用户启动 APP 时，自动广播各自的设备名、IP 及自定义昵称/头像，并在界面展示所有在线用户列表。
- **零拷贝传输模块**：使用多线程 TCP 建立并行数据管道，配合 `mmap`（内存映射文件）与零拷贝技术，打满局域网带宽。支持针对特定用户发起文件传输。
- **iOS Liquid Glass UI 模块**：基于毛玻璃特效 (backdrop-filter) 及液体黏滞 (Liquid Snapping) 动画的高颜值交互界面。

### 2.2 页面结构与 iOS 26 视觉规范
- **主视图 (Main View)**：
  - **磨砂玻璃侧边栏**：背景模糊半径 `35px`，白光遮罩不透明度 `12%`，圆角 `24px`，带 `1px` 顶部白色高光微透边框。
  - **设备雷达区**：正中央为水波纹雷达扫描动效，展示局域网内所有在线用户卡片（显示昵称与专属头像）。
- **文件选择与发送逻辑**：
  - **主动选择模式**：用户点击特定目标用户的头像卡片，程序立即唤起 Windows 原生文件资源管理器窗口（可访问 C/D/E 盘等全盘路径）。
  - **多选支持**：原生窗口中支持 Shift 连续多选、Ctrl 离散多选等 Windows 常见文件选择逻辑。
  - **待发预览与发送**：选择完成后，返回 APP 主页面。主页面以 iOS 风格列表展示待发送文件清单、文件总数及总体积。用户确认无误后点击“发送”，即启动多线程传输。
- **拖拽投放区 (Drop Zone)**：
  - 选中设备后，拖拽文件至设备卡片上。卡片在文件临近时产生“液体吸附”弹性动效。
- **灵动岛传输状态栏 (Dynamic Island)**：
  - 界面顶部或底部悬浮的药丸形状态条。开始传输时平滑展开，显示速度、进度条与文件缩略图；传输完成时弹性缩回。

### 2.3 异常用例
- **网络中断/切换**：传输中网络断开，自动暂存已传分片，提示“网络中断”，重连后支持一键断点续传。
- **磁盘容量预检**：接收端在同意接收前，后台预检目标分区剩余容量，空间不足时立即拦截并提示。
- **同名文件冲突**：弹窗提示“覆盖”、“保留两者（重命名）”或“跳过”。

---

## 3. 技术架构与传输优化方案

为了确保“速度快”与“iOS风格UI”，方案结合了高性能 Rust 传输后端与 Webview2 渲染层的优势。

### 3.1 技术选型
- **前端 (UI 层)**：Tauri + React/Vue3 + CSS backdrop-filter / TailwindCSS。
  - *优势*：Web 渲染层能够最轻松地利用 CSS 滤镜、SVG 滤镜和 canvas 实现超高还原度的 iOS 毛玻璃与液体黏滞特效。
- **后端 (数据层)**：Rust (Tauri Core) + Tokio (异步运行时)。
  - *优势*：Rust 能够无缝调用操作系统的 `mmap` (Windows 下为 `CreateFileMapping`/`MapViewOfFile`)，且多线程 TCP Socket 性能优异，包体积仅十几MB。

### 3.2 速度优化三要素（零拷贝与并行通道）
1. **内存映射 (mmap)**：
   - 发送端和接收端直接将大文件映射到虚拟内存中，避免传统 `read`/`write` 系统调用在内核缓冲区与用户缓冲区之间来回拷贝数据。
2. **多 TCP 连接并发分片**：
   - 根据文件大小自动切分为 `4MB - 16MB` 的分片。
   - 建立 4-8 个 TCP 并发管道，并行传输分片，解决局域网单 TCP 连接因丢包/窗口滑动导致的带宽抖动。
3. **顺序重组与零拷贝写入**：
   - 接收端通过 mmap 预先分配与源文件等大的磁盘空间，各通道接收到的分片直接写入其对应内存偏移量 (Offset)，无须在应用层进行二次拼接重组。

### 3.3 核心数据结构与接口

#### 传输协议元数据 (JSON)
```json
{
  "packet_type": "handshake | metadata | chunk | complete",
  "device_id": "UUID-String",
  "file_info": {
    "name": "filename.mp4",
    "size": 10737418240,
    "hash": "SHA-256-String",
    "total_chunks": 640
  }
}
```

---

## 4. 强制收尾：需求风险与优化替代方案

- **风险 1：Windows 本地防火墙默认拦截 UDP 广播/组播，导致设备无法发现。**
  - *优化方案*：在 iOS 风格界面提供“IP手动连接 / 扫描对端二维码”作为备用机制，同时在首次启动时引导用户授权防火墙。
- **风险 2：Wi-Fi 环境下多连接并发容易造成信道拥堵，速度反而下降。**
  - *优化方案*：后台进行实时网络测速与信道质量评估。若延迟抖动过大，动态收缩并发 TCP 管道数量（如从 8 缩减至 4 或 2）。
- **风险 3：大文件夹小文件过多（如数万个碎图片）导致磁盘 I/O 成为速度瓶颈。**
  - *优化方案*：发送前在内存中对碎小文件进行轻量打包（不压缩），生成一个大流进行传输，接收端边接收边还原解包。
